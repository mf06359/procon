# nushell console appearance for the CP nix-shell.
# The prompt itself is themed in .config/starship.toml; shell.nix generates and
# sources starship's nu integration at startup. Put extra nushell-only visual
# tweaks here.
$env.config.table.mode = "rounded"
